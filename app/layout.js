export const metadata = {
  title: "Via CommandDesk V4.1 Pilot",
  description: "Sistema operacional de gestão empresarial — Via Networks"
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
